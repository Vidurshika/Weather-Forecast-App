#  to store API keys securely using environment variables instead of directly in the code.
# The variable name must start with REACT_APP_ because Create React App requires this convention.
# To avoid exposing your API key in public repositories, open .gitignore and add:This ensures the .env file is not uploaded to GitHub.    

REACT_APP_WEATHER_API_KEY=33ab77......-this line is included in .env 

#Also  this is the .env file outside of the src like package.json file

# https://api.openweathermap.org/data/2.5/weather?q=Colombo&units=metric&appid=33ab7779f899d00f5f32f4652ddd7dcd
